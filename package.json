{
  "name": "smallbear",
  "description": "The bare minimum css for kicking off a project.",
  "version": "1.0.4",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "watch": "gulp && gulp watch",
    "build": "gulp"
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-concat": "^2.6.1",
    "gulp-csso": "^3.0.1",
    "gulp-sass": "^4.0.2"
  }
}
